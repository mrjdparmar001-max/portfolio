# Render keep-awake workflow removed.
name: Keep Render Awake (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Render keep-awake is disabled"

